<div class="p-comment__item">
  <p><%= simple_format(comment.comment) %></p>
  <div class="p-comment__bottomLine">
    <span><%= comment.name %></span>
    <span><%= comment.created_at.to_s(:datetime_base) %></span>
    <span><%= link_to '削除', [@activity,comment], class:'btn btn-outline-info', method: :delete, 
    data: {
    confirm: "コメントを削除しますか？",
    cancel: "キャンセル",
    commit: "削除する", title: "削除の確認"} %></span>
  </div>
</div>